from django.urls import path

from .import views

app_name = 'drivers'
urlpatterns = [
    path("oregister", views.register, name = 'register'),
    path("edit", views.edit, name = 'edit'),
    path("delete", views.delete, name = 'delete'),
]
